void main() {
  // não é recomendado utilizar
  var nome = 'Isaias';
  nome.toUpperCase();

  // assume qualquer dado usar quando é extremamente nessesário
  dynamic variavel = 'Daniel';
  variavel = 3;
  print(variavel);

  // Utilizado para número quando não definido 
  num numero = 2.5;
  print(numero);
}
